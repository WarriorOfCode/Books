<div class="books">
	<% books.forEach(function(book) { %>
		<div class="col-sm-3">
			<a href='/book/<%= book.id %>'>
				<img src="<%= book.image_url %>">
				<div><%= book.Name %></div>
			</a>
		</div>
	<% }) %>
</div>